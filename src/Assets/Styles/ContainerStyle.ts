import { makeStyles } from '@mui/styles';

export const useActionContainerStyles = makeStyles({
  default: {
    flexWrap: 'wrap',
    backgroundColor: '#fff',
    padding: 20,
    border: '1px solid #e2e2e2',
    borderRadius: 12,
    height: '84vh',
    overflow: 'auto',
  },
});

export const useTableContainerStyles = makeStyles({});
